# in the coe below, an array containing different types of colors is assigned to colors
colors = ['red', 'yellow', 'purple', 'green']
# use array#each to iterate over colors and print each element
colors.each {|color| puts "I am the color #{color}" }