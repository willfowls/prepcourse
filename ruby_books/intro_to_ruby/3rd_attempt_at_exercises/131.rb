# write an if statement that prints the sun is so bright if sun equals visible
# write unless statement that prints the clouds are blocking the sun unless sun equal svisible

sun = ['visible', 'hidden'].sample

if sun == 'visible'
    puts 'sun is so bright'
end
unless sun == 'visible'
    puts 'cloudy skies'
end

# check