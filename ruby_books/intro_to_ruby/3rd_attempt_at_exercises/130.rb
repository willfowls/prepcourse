# write an unless statement that print the clouds are blocking the sun unless the sun equals visible

sun = ['visible', 'hidden'].sample

unless sun == 'visible'
    puts 'the clouds are blocking the sun'
else 
    puts 'it is so bright out'
end

# check