# write a method that counts down to zero using recursion

def recursion(n)
    if n <= 0
        puts n
    else
        puts n
        recursion(n-1)
    end
end

recursion(20)

# no check but close 