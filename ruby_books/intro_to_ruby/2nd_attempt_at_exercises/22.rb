# when you run the following code you get the error message below the code

def equal_to_four(x)
    if x == 4
      puts "yup"
    else
      puts "nope"
  end   
end

puts equal_to_four(4)

#exercise.rb:8: syntax error, unexpected end-of-input, expecting keyword_end
# already fixed it so you can't see the error but basically he didn't have the 2nd end and also he wasn't using puts before trying
# the method so you couldn't see it