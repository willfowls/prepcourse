# write two methods, one that returns 'hello' and one that returns the string 'world' then print both strings using puts
#combine them into one sentence

def hello
    'hello'
end

def world
    'world'
end
puts "#{hello} #{world}"